<%= form_with(model: vocabulary) do |form| %>
  <% if vocabulary.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vocabulary.errors.count, "error") %> prohibited this vocabulary from being saved:</h2>

      <ul>
        <% vocabulary.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :japanese, "日文", style: "display: block" %>
    <%= form.text_field :japanese %>
  </div>

  <div>
    <%= form.label :kanji, "漢字/英文/外來語", style: "display: block" %>
    <%= form.text_field :kanji %>
  </div>

  <div>
    <%= form.label :chinese, "中文", style: "display: block" %>
    <%= form.text_field :chinese %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
